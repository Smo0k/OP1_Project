package main.commands;

/**
 * Represents the direction in which an image can be rotated.
 */
public enum RotateDirection {

    /**
     * Rotates the image 90 degrees counterclockwise.
     */
    LEFT,

    /**
     * Rotates the image 90 degrees clockwise.
     */
    RIGHT
}